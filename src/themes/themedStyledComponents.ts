import * as styledComponents from "styled-components";

import { FrismAdminTheme } from "./normal";

export const {
  default: styled,
  css,
  keyframes,
  ThemeProvider,
  ThemeContext,
  createGlobalStyle,
} = styledComponents as unknown as any;
